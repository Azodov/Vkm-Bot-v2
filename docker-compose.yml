services:
  bot:
    build: .
    container_name: vkm-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./cookies.txt:/app/cookies.txt:ro
    # agar loglarni kichraytirish kerak bo'lsa:
    logging:
      options:
        max-size: "10m"
        max-file: "3"
